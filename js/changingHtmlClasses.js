// changing HTML classes | className | classList

// className
first.className = "text-black red";

// classList
first.classList.add("red"); // add a class

first.classList.remove("red"); // removes a class

first.classList.toggle("red"); // adds the class if it does'nt exist, otherwise removes it

first.classList.contain("red"); // checks for the given class returns true | false
